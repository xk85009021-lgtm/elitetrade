services:
  - type: web
    name: elitetrade
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server/server.js
    healthCheckPath: /api/public/content
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production